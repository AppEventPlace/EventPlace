export default [
  {
    isUserAuthenticated: true,
  },
];
/*import React, { useState } from "react";

const MenuState = () => {
  const [isUserAuthenticated, setIsUserAuth] = useState(true);

  const ChangeState = ({ state }) => {
    setIsUserAuth(state);
  };
  //return isUserAuthenticated;
};
export default MenuState;

import { Text, View } from "react-native";

const [isUserAuthenticated, setIsUserAuth] = useState(false);
function EstadoAuth(Estado) {
  setIsUserAuth(Estado);
}
function Autenticado() {
  return isUserAuthenticated;
}
export default Autenticado;
*/

/*const Autenticar = (valor) => {
  const [isUserAuthenticated, setIsUserAuth] = useState("false");

  function AutenticarCliente() {
    setIsUserAuth((isUserAuthenticated = valor));
  }
};*/
